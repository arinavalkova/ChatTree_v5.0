package host.connections.refuse;

import host.Host;
import main.DataStream;

public class ChildRefuse implements ConnectionRefuse{
    @Override
    public void changeConnection(Host host, DataStream dataStream) {

    }
}
